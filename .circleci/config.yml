version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker pull vulnerables/web-dvwa #vulnerables/web-dvwa is a demo vulnerable image used for testing here
      - run: curl https://raw.githubusercontent.com/ABHINAVKISHOREGV/clair-practice/master/clair-scan.sh > clair.sh
      - run: sh clair.sh -p vulnerables/web-dvwa
